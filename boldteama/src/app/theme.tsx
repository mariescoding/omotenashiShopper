'use client';

import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#EFEFEF',
      light: '#FFFFFF',
      dark: '000000'
    },
    secondary: {
      main: '#4A4CE0',
      light: '#DCDDF6',
     
    }
   
  },
  
});
